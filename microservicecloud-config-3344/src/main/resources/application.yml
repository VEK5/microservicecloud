server: 
  port: 3344 
  
spring:
  application:
    name:  microservicecloud-config                             # 本服务的名称
  cloud:
    config:
      server:
        git:
          uri: git@github.com:VEK5/microservicecloud-config.git # GitHub上面的git仓库名字
 

